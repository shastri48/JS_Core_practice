// Name: join
// work: Converts all elements in array into a string separated by separator.
// arguments: Array and seperator


export default function join(arr, seperator){
  return arr.join(seperator);
}